# --- API ---
fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.4
python-dotenv==1.0.1

# --- DB ---
psycopg[binary]==3.1.19

# --- NLP / ML ---
spacy==3.7.5
transformers==4.42.3
datasets==2.20.0
accelerate==0.31.0
sentence-transformers==3.0.1
torch==2.3.1
rapidfuzz==3.9.4
keybert==0.8.5
scikit-learn==1.5.0
readability-lxml==0.8.1
requests==2.32.3

# --- Live ingestion (Phase 1) ---
# GDELT Doc 2.0 API needs no key; requests fetches both the GDELT query and
# each article's HTML, which readability-lxml then reduces to body text.
lxml==5.2.2

# --- Dev / eval ---
pytest==8.2.2
pandas==2.2.2
