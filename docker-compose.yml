services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_PASSWORD: dev
    ports:
      - "5432:5432"
    volumes:
      - newslens_pgdata:/var/lib/postgresql/data

volumes:
  newslens_pgdata:
